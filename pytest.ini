[pytest]
testpaths = tests
addopts = -v --tb=short --durations=5
markers =
    smoke: quick sanity checks - file exists, imports work (run every time)
    core: data structure and logic tests (run after code changes)
    live: requires live cTrader API connection (slow, run before releases)
    slow: takes more than 5 seconds
